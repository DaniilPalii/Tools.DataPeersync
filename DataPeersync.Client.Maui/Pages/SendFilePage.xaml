<?xml version="1.0" encoding="utf-8"?>

<ContentPage
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="DataPeersync.Client.Maui.Pages.SendFilePage"
	Title="Send File">

	<ScrollView>
		<VerticalStackLayout>

			<HorizontalStackLayout>
				<Label Text="IP:" />
				<Entry x:Name="IpEntry" />
			</HorizontalStackLayout>

			<HorizontalStackLayout>
				<Label Text="Port:" />
				<Entry x:Name="PortEntry" />
			</HorizontalStackLayout>

			<HorizontalStackLayout>
				<Label Text="File Path:" />
				<Label x:Name="FilePathLabel" />
				<Button x:Name="BrowseButton" Text="Browse" />
			</HorizontalStackLayout>

			<Button x:Name="SendFileButton" Text="Send File" />

			<Label x:Name="StatusLabel" />

		</VerticalStackLayout>
	</ScrollView>
</ContentPage>
